###############################################################################
# EMOS Docker Compose
# ---------------------------------------------------------------------------
# Defines containerised services that the EMOS platform talks to over HTTP.
# Each heavy/GPU-dependent model lives in its own container.
#
# Usage:
#   docker compose up -d                  # CPU-only (no GPU)
#   docker compose up -d --build          # rebuild images first
#   docker compose --profile gpu up -d    # with NVIDIA GPU passthrough
#
# The main EMOS backend (Flask on :5001) runs on the host and reaches
# these services via http://localhost:<port>.
###############################################################################

services:
  # -----------------------------------------------------------------------
  # MatterGen — crystal-structure generator (Microsoft Research)
  # -----------------------------------------------------------------------
  mattergen:
    build:
      context: ./Information_Units/Generators/MattergenBaseModel/docker
      dockerfile: Dockerfile
    container_name: emos-mattergen
    ports:
      - "${MATTERGEN_PORT:-8100}:8000"
    volumes:
      # Cache HuggingFace models so they survive container restarts
      - ./data/mattergen/hf_cache:/root/.cache/huggingface
    environment:
      # Set to "cpu" to force CPU inference (useful for testing)
      - MATTERGEN_DEVICE=${MATTERGEN_DEVICE:-auto}
    restart: unless-stopped

    # Uncomment the block below (or use --profile gpu) for GPU passthrough
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  # -----------------------------------------------------------------------
  # GPU profile variant — same service but with GPU reservation
  # Activate with:  docker compose --profile gpu up -d
  # -----------------------------------------------------------------------
  mattergen-gpu:
    extends:
      service: mattergen
    container_name: emos-mattergen-gpu
    profiles: ["gpu"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
  



  # -----------------------------------------------------------------------
  # MatterSim — ML interatomic potential predictor (Microsoft Research)
  # -----------------------------------------------------------------------
  mattersim:
    build:
      context: ./Information_Units/Predictors/Mattersim/docker
      dockerfile: Dockerfile
    container_name: emos-mattersim
    ports:
      - "${MATTERSIM_PORT:-8300}:8000"
    environment:
      - MATTERSIM_LOG_LEVEL=${MATTERSIM_LOG_LEVEL:-DEBUG}

  mattersim-gpu:
    extends:
      service: mattersim
    container_name: emos-mattersim-gpu
    profiles: ["gpu"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]


  # -----------------------------------------------------------------------
  # CHGNet — charge-informed interatomic potential predictor
  # -----------------------------------------------------------------------
  chgnet:
    build:
      context: ./Information_Units/Predictors/Chgnet/docker
      dockerfile: Dockerfile
    container_name: emos-chgnet
    ports:
      - "${CHGNET_PORT:-8400}:8000"
    environment:
      - CHGNET_LOG_LEVEL=${CHGNET_LOG_LEVEL:-DEBUG}
      - CHGNET_MODEL_NAME=${CHGNET_MODEL_NAME:-0.3.0}
      - CHGNET_DEVICE=${CHGNET_DEVICE:-}

  chgnet-gpu:
    extends:
      service: chgnet
    container_name: emos-chgnet-gpu
    profiles: ["gpu"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]


  # -----------------------------------------------------------------------
  # GBFS — material-property predictor (LightGBM + matminer)
  # -----------------------------------------------------------------------
  # Exposes a FastAPI server for predicting material properties from CIF data.
  #   POST /predict/{property}   – predict a single property
  #   POST /batch-predict        – predict multiple properties at once
  #   GET  /info                 – list supported properties
  #   GET  /health               – health check
  #   GET  /docs                 – interactive API docs (Swagger)
  # -----------------------------------------------------------------------
  gbfs-pred:
    build:
      context: .
      dockerfile: Information_Units/Predictors/Gbfs/docker/Dockerfile
    image: gbfs-pred:latest
    container_name: emos-gbfs-pred
    ports:
      - "${GBFS_PORT:-8200}:8000"
    environment:
      PYTHONUNBUFFERED: 1
      PYTHONDONTWRITEBYTECODE: 1
      GBFS_HOST: 0.0.0.0
      GBFS_PORT: 8000
      GBFS_LOG_LEVEL: INFO
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/ready')"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

  gbfs-2d:
    build:
      context: .
      dockerfile: Information_Units/Predictors/Gbfs2d/docker/Dockerfile
    image: gbfs2d-pred:latest
    container_name: emos-gbfs-2d
    ports:
      - "${GBFS2D_PORT:-8201}:8000"
    environment:
      PYTHONUNBUFFERED: 1
      PYTHONDONTWRITEBYTECODE: 1
      GBFS2D_HOST: 0.0.0.0
      GBFS2D_PORT: 8000
      GBFS2D_LOG_LEVEL: INFO
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/ready')"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
